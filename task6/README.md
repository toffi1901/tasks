# Задача 6 

## Сборка
- `make` – собирает исполняемый файл `task6`
- Поддерживаются переменные `CFLAGS` 

## Очистка
- `make clean` – удаляет `task6` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Использование (пример запуска)
   ```bash
   ./task6 test_file
