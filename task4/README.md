# Задача 4

## Сборка
- `make` – собирает исполняемый файл `task4`
- Поддерживаются переменные `CFLAGS`

## Очистка
- `make clean` – удаляет `task4` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Использование (пример запуска)
   ```bash
   ./task4 -tm -s --elbrus=16c file0 --elbrus=2c3 file1
