# Задача 3

## Сборка
- `make` – собирает исполняемый файл `task3`
- Поддерживаются переменные `CFLAGS`

## Очистка
- `make clean` – удаляет `task3` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Использование (пример)
   ```bash
   ./task3 in.txt out.txt plain
