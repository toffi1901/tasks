# Задача 9

## Сборка
- `make` – собирает исполняемый файл `task9`
- Поддерживаются переменные `CFLAGS` и `LDFLAGS` 

## Очистка
- `make clean` – удаляет `task9` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Пример запуска
```bash
./task9
1 2 3 0 4 5 6 0
