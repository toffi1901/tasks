# Задача 7

## Сборка
- `make` – собирает исполняемый файл `task7`
- Поддерживаются переменные `CFLAGS` и `LDFLAGS` 

## Очистка
- `make clean` – удаляет `task7` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Использование
1. Создайте FIFO (если не создан автоматически программой):
   ```bash
   ./task7 test_file
