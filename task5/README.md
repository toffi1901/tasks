# Задача 5 

## Сборка
- `make` – собирает исполняемый файл `task5`
- Поддерживаются переменные `CFLAGS` 

## Очистка
- `make clean` – удаляет `task5` и объектные файлы (`*.o`)

## Зависимости
- Компилятор `gcc`

## Использование (пример запуска)
   ```bash
   ./task5
